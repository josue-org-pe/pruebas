#include<iostream>
#include<fstream>
#include<cmath>
#include<string>
#include<vector>
using namespace std;
void lecture();
int main()
{
    string correos;
    cout<<"Ingrese el nombre del archivo que condtiene el (Nombre,Edad,Email)";
    ifstream archivo(correos);
    if (archivo.fail())
    {
        cout<<"No se puede abrir el archivo ,asegurate de tener el archivo en la misma ruta del exe";
        return 1;
    }
    int datos[]
}+
}
